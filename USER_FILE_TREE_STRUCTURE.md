# 📁 User-Related File Tree Structure (DriveCare)

## 🏠 User Pages & Navigation
```
src/app/(app)/
├── edit-profile-user/
│   └── page.tsx (14.7KB) - หน้าแก้ไขโปรไฟล์ผู้ใช้
├── job-detail-user/
│   └── page.tsx (22.9KB) - หน้ารายละเอียดงานสำหรับผู้ใช้
├── user-booking/
│   └── page.tsx (14.2KB) - หน้าจองรถสำหรับผู้ใช้
├── user-list-reserve/
│   └── page.tsx (16.7KB) - หน้ารายการการจองของผู้ใช้
├── user-payment/
│   └── page.tsx (18.9KB) - หน้าชำระเงินของผู้ใช้
├── health-user-booking/
│   └── page.tsx (16.5KB) - หน้าจองด้านสุขภาพของผู้ใช้
├── notifications/
│   └── page.tsx (5.1KB) - หน้าการแจ้งเตือนสำหรับผู้ใช้
└── settings/
    └── page.tsx (4.6KB) - หน้าตั้งค่าสำหรับผู้ใช้ (โปรไฟล์, ออกจากระบบ)
```

## 🔐 User Registration
```
src/app/register-user/
└── page.tsx (13.1KB) - หน้าลงทะเบียนผู้ใช้ใหม่
```

## 📡 User API Endpoints
```
src/app/api/
├── auth/users/
│   ├── login/ - API ล็อกอินผู้ใช้
│   └── register/ - API ลงทะเบียนผู้ใช้
├── user-controller/
│   ├── edit-profile/
│   │   └── route.ts (1.5KB) - API แก้ไขข้อมูลผู้ใช้
│   └── upload-image/
│       └── route.ts (2.6KB) - API อัพโหลดรูปผู้ใช้
├── users/
│   └── route.ts (1.4KB) - API ดึงข้อมูลผู้ใช้
├── booking/users/
│   ├── [id]/
│   │   ├── cancel-booking/
│   │   │   └── route.ts (4.8KB) - API ยกเลิกการจอง
│   │   └── detail-booking/
│   │       └── route.ts (2.1KB) - API รายละเอียดการจอง
│   ├── comfirm-booking/
│   │   └── route.ts (5.4KB) - API ยืนยันการจอง
│   ├── my-bookings/
│   │   └── route.ts (1.6KB) - API รายการการจองของฉัน
│   └── payments/
│       └── route.ts (8.0KB) - API การชำระเงิน
├── health-bookinng/
│   ├── create-health/
│   │   └── route.ts (2.5KB) - API สร้าง/อัปเดตข้อมูลสุขภาพผู้ใช้
│   └── get-health/
│       └── route.ts (1.1KB) - API ดึงข้อมูลสุขภาพผู้ใช้
└── reports/users/
    └── route.ts (3.8KB) - API รายงานผู้ใช้
```

## 🎨 User Components
```
src/components/
├── user/
│   └── StatusTrackerCard.tsx (4.5KB) - Component ติดตามสถานะการเดินทาง
├── navigation-menu/
│   ├── nav-menu.ts - เมนูนำทางหลัก (รองรับ user/driver)
│   └── bottom-navbar.tsx (2.9KB) - Bottom navigation สำหรับผู้ใช้
└── admin/manager-users/
    ├── AdminUsersTable.tsx - ตารางจัดการผู้ใช้ (Admin)
    └── EditUserModal.tsx - Modal แก้ไขผู้ใช้ (Admin)
```

## 🔄 State Management
```
src/context/
└── UserContext.tsx (3KB) - Context จัดการสถานะผู้ใช้ทั่วระบบ

src/store/
└── notification.state.ts - State จัดการการแจ้งเตือนสำหรับผู้ใช้
```

## 📝 Type Definitions
```
src/types/
├── profile/
│   ├── base.ts - Base interface สำหรับโปรไฟล์
│   ├── user.ts - User profile interface
│   └── driver.ts - Driver profile interface
├── user/
│   ├── bookings.ts (26 lines) - Type สำหรับการจอง
│   └── health-bookinng.ts (30 lines) - Type สำหรับการจองด้านสุขภาพ
├── admin/
│   └── manager-users.ts (66 lines) - Admin user management types
├── auth/
│   └── line.ts - LINE authentication types
├── notification.ts - Type สำหรับการแจ้งเตือนผู้ใช้
├── profile.ts - Export รวม profile types
└── forms/auth.ts (35 lines) - Form types สำหรับ authentication
```

## 📋 Constants & Policies
```
src/constants/policy/
└── user-terms.ts (48 lines) - ข้อกำหนดและเงื่อนไขสำหรับผู้ใช้
```

---

## 🔑 Key User Features:

### **🏠 Main User Navigation**
- **user-booking** - หน้าจองรถหลัก
- **user-list-reserve** - รายการการจองทั้งหมด  
- **user-payment** - การชำระเงิน
- **health-user-booking** - การจองด้านสุขภาพ
- **edit-profile-user** - แก้ไขโปรไฟล์
- **job-detail-user** - รายละเอียดงาน
- **notifications** - การแจ้งเตือนสำหรับผู้ใช้
- **settings** - ตั้งค่าระบบ (โปรไฟล์, ออกจากระบบ)

### **🔐 Authentication Flow**
- **register-user** - ลงทะเบียนผู้ใช้ใหม่
- **auth/users/** - API การยืนยันตัวตน
- **UserContext** - State management หลัก

### **📋 Complete Booking System**
- **booking/users/** - API จัดการการจองทั้งหมด
  - ยกเลิกการจอง
  - ดูรายละเอียด
  - ยืนยันการจอง
  - รายการของฉัน
  - การชำระเงิน

### **🏥 Health Management**
- **health-bookinng/** - API จัดการข้อมูลสุขภาพผู้ใช้
  - สร้าง/อัปเดตข้อมูลสุขภาพ
  - ดึงข้อมูลสุขภาพผู้ใช้

### **👤 User Management**
- **users/** - API ดึงข้อมูลผู้ใช้
- **user-controller/** - API ควบคุมผู้ใช้
- **profile types** - โครงสร้างข้อมูลผู้ใช้

### **⚙️ Admin Features**
- **admin/manager-users/** - จัดการผู้ใช้สำหรับ Admin
- **reports/users/** - รายงานผู้ใช้

---

*โครงสร้างนี้ครอบคลุมทุกส่วนหลักที่เกี่ยวข้องกับผู้ใช้ในระบบ DriveCare*
